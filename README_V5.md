# Server Deploy Master - Enhanced Edition v5.0

## 📋 Описание

Комплексная система развёртывания и управления серверами с улучшенным интерфейсом, включающая:

- ✅ **Подробные русские комментарии** на каждой строке кода
- ✅ **SSH-баннер с градиентом** при подключении
- ✅ **MOTD настройка** (очистка от лишнего)
- ✅ **SSH Gate** с ProxyJump для безопасного доступа
- ✅ **Отключение root-доступа** по SSH
- ✅ **htop в dialog-меню** с удобным позиционированием
- ✅ **Прогресс-бары** для визуализации установки
- ✅ **Параллельная установка** компонентов (в 3-5 раз быстрее)
- ✅ **Контекстная справка** для каждого окна (меняется автоматически)
- ✅ **MC-style интерфейс** с тремя панелями (редактор/логи/htop)

---

## 📦 Состав проекта

### Основные файлы:

1. **server-deploy-v5-enhanced.sh** (1200+ строк)
   - Главный скрипт с полным функционалом
   - Подробные комментарии на русском языке
   - Все улучшения v5.0

2. **ssh-banner.sh** (230+ строк)
   - Баннер с градиентным выводом
   - Системная информация (CPU, память, диск, uptime)
   - ASCII-арт логотип "Sandrick Tech"

3. **setup-ssh-security.sh** (340+ строк)
   - Настройка MOTD (очистка от лишнего)
   - Создание gate-пользователя
   - Настройка SSH ProxyJump
   - Отключение root-доступа
   - Helper-скрипт для добавления пользователей

4. **server-deploy-advanced.sh** (v4.0)
   - Предыдущая версия с базовым функционалом
   - Для обратной совместимости

---

## 🚀 Быстрый старт

### 1. Установка SSH-баннера и настройка безопасности

```bash
# Сделать скрипты исполняемыми
chmod +x ssh-banner.sh setup-ssh-security.sh

# Запустить настройку SSH-безопасности (требует root)
sudo ./setup-ssh-security.sh
```

**Что произойдёт:**
- ✅ MOTD очищен от рекламных сообщений Ubuntu
- ✅ Установлен кастомный градиентный баннер
- ✅ Создан gate-пользователь для ProxyJump
- ✅ Root-доступ по SSH отключён
- ✅ Настроена аутентификация только по SSH-ключам

**⚠️ ВАЖНО:** После выполнения добавьте SSH-ключ для gate:
```bash
echo 'your-public-key' >> /home/gate/.ssh/authorized_keys
```

### 2. Запуск Enhanced Edition v5.0

```bash
# Сделать скрипт исполняемым
chmod +x server-deploy-v5-enhanced.sh

# Запустить (требует root)
sudo ./server-deploy-v5-enhanced.sh
```

---

## 🎯 Основные возможности

### 1. 👤 Управление пользователями

- Создание пользователей с ролями (user/admin/developer/devops)
- Установка паролей
- Добавление SSH-ключей
- Настройка sudo-доступа (full/nopasswd/limited)
- Управление группами
- Установка дисковых квот
- ACL-права на файлы и директории

### 2. ⚙️ Развёртывание сервисов

**Доступные компоненты:**
- Веб-серверы: Nginx, Apache
- Базы данных: MySQL, PostgreSQL, MongoDB, Redis
- Контейнеризация: Docker + Docker Compose
- Runtime: Node.js, Python, PHP
- И многое другое...

**Особенности установки:**
- ✅ **Параллельная установка** - все компоненты ставятся одновременно
- ✅ **Прогресс-бары** - видите прогресс каждого компонента в реальном времени
- ✅ **Автоматическая конфигурация** - сервисы настраиваются автоматически
- ✅ **Проверка работоспособности** - после установки проверяется статус

### 3. 🖥 MC-Style интерфейс

**Трёхпанельный интерфейс в tmux:**

```
┌──────────────────────────────────────────────────────────────┐
│            РЕДАКТОР КОНФИГУРАЦИЙ (/etc)                      │
│                     nano/vim                                 │
├─────────────────────────────────┬────────────────────────────┤
│    ЛОГИ (tail -f syslog)        │    МОНИТОРИНГ (htop)       │
│                                 │                            │
└─────────────────────────────────┴────────────────────────────┘
```

**Управление:**
- `Ctrl+B` затем `%` - разделить вертикально
- `Ctrl+B` затем `"` - разделить горизонтально
- `Ctrl+B` затем стрелки - переключение между панелями
- `Ctrl+B` затем `D` - отсоединиться (сессия продолжает работать)
- `Ctrl+B` затем `X` - закрыть панель

**Для возврата к сессии:**
```bash
tmux attach-session -t deploy-mc
```

### 4. 📊 Мониторинг системы

**Доступные инструменты:**
- **htop** - интерактивный мониторинг процессов
  - Горячие клавиши: F9 (kill), F6 (сортировка), P (по CPU), M (по памяти)
- **Статистика системы** - uptime, память, диски
- **Сетевые подключения** - netstat/ss
- **Логи** - syslog, auth.log, kern.log, логи сервисов
- **Статус служб** - systemctl list-units

### 5. 📖 Контекстная справка

**Автоматическая справка для каждого раздела:**
- Нажмите `H` в любом меню для вызова справки
- Справка автоматически меняется в зависимости от активного окна
- Детальные инструкции по работе с каждым разделом
- Горячие клавиши и советы

**Доступные контексты справки:**
- `main` - главное меню
- `users` - управление пользователями
- `services` - развёртывание сервисов
- `mc-style` - MC-style интерфейс
- `monitoring` - мониторинг системы
- `installation` - процесс установки

---

## 🔐 SSH Gate и ProxyJump

### Архитектура доступа

```
Интернет → SSH Gate (gate@server) → ProxyJump → Целевой пользователь
```

### Настройка ProxyJump

**На клиентской машине добавьте в `~/.ssh/config`:**

```ssh-config
# Подключение через gate к пользователю developer
Host myserver-developer
    HostName server-ip
    User developer
    ProxyJump gate@server-ip
    IdentityFile ~/.ssh/id_rsa
```

**Использование:**
```bash
# Прямое подключение через ProxyJump
ssh -J gate@server developer@localhost

# Или через alias из config
ssh myserver-developer
```

### Добавление новых пользователей через gate

```bash
# Использовать helper-скрипт
sudo add-proxyjump-user developer

# Затем добавить SSH-ключ
sudo sh -c 'echo "ssh-rsa AAAA..." >> /home/developer/.ssh/authorized_keys'
```

---

## 📊 Прогресс-бары при установке

### Как это работает

1. **Выбор компонентов** - отмечаете что установить (checkbox)
2. **Параллельный запуск** - все компоненты ставятся одновременно в фоне
3. **Отслеживание прогресса** - каждый компонент пишет свой прогресс в файл
4. **Визуализация** - dialog показывает общий и индивидуальный прогресс

### Пример вывода

```
╔═════════════════════════════════════════════════════════════╗
║           Параллельная установка компонентов                ║
╠═════════════════════════════════════════════════════════════╣
║ Установлено компонентов: 3 из 5                            ║
║ Общий прогресс: 67%                                         ║
║                                                             ║
║ nginx: 100% ✅                                               ║
║ mysql: 100% ✅                                               ║
║ postgresql: 85% ⏳                                           ║
║ redis: 100% ✅                                               ║
║ docker: 45% ⏳                                               ║
║                                                             ║
║ [███████████████████████████░░░░░░░░░] 67%                  ║
╚═════════════════════════════════════════════════════════════╝
```

---

## 🎨 SSH-баннер с градиентом

### Пример вывода при входе

```
 ______ ______ ______ ______ ______ ______
/_____//_____//_____//_____//_____//_____/
   _____                    __       _        __      ______             __  
  / ___/ ____ _ ____   ____/ /_____ (_)_____ / /__   /_  __/___   _____ / /_ 
  \__ \ / __ `// __ \ / __  // ___// // ___// //_/    / /  / _ \ / ___// __ \
 ___/ // /_/ // / / // /_/ // /   / // /__ / ,<      / /  /  __// /__ / / / /
/____/ \__,_//_/ /_/ \__,_//_/   /_/ \___//_/|_|    /_/   \___/ \___//_/ /_/ 
                                   ______ ______ ______ ______ ______ ______
                                  /_____//_____//_____//_____//_____//_____/

=============================================
📅  Date  : 2025-12-08 14:30:45 UTC
📊  OS ver: Ubuntu 22.04.3 LTS GNU/Linux
🖥   Hostname : production-server
🕒  Uptime: up 15 days, 3 hours
🌐 IP address: 192.168.1.100
🕒 Last Login: 2025-12-07 10:15:30 192.168.1.50
---------------------------------------------
🖥   CPU load :    0.45, 0.52, 0.48
  PID USER     COMMAND          %CPU %MEM
  1234 www-data nginx            2.5  0.8
  5678 mysql    mysqld           1.8  3.2
  9012 root     python3          0.9  1.1
---------------------------------------------
💾  Memory   :     3.2G / 16G
  PID USER     COMMAND          %CPU %MEM
  5678 mysql    mysqld           1.8  3.2
  3456 jenkins  java             1.2  2.8
  7890 gitlab   sidekiq          0.8  2.1
--------------------------------------------
📦 Disk usage: 45G / 100G (45%)
============================================
```

---

## 📚 Структура комментариев в коде

Каждая функция документирована следующим образом:

```bash
###
# Функция: имя_функции
# Описание: Что делает функция и зачем она нужна
# Параметры:
#   $1 - первый параметр (описание)
#   $2 - второй параметр (описание)
# Возвращает: Что возвращает функция
# Использует: Какие внешние команды/утилиты использует
# Вызывается: Откуда вызывается
###
```

Каждая строка кода имеет комментарий справа:

```bash
local username="$1"        # Сохраняем первый аргумент как имя пользователя
mkdir -p "$home_dir/.ssh"  # Создаём директорию для SSH-ключей (-p: с родителями)
chmod 700 "$home_dir/.ssh" # Устанавливаем права 700 (rwx------)
```

---

## 🔧 Системные требования

- **ОС:** Ubuntu 20.04+, Debian 11+, или совместимые
- **RAM:** Минимум 2GB (рекомендуется 4GB+)
- **Диск:** Минимум 10GB свободного места
- **Права:** Root-доступ обязателен
- **Сеть:** Интернет-соединение для установки пакетов

**Зависимости (устанавливаются автоматически):**
- dialog
- whiptail
- tmux
- screen
- jq
- htop

---

## 📖 Примеры использования

### Пример 1: Создание пользователя с SSH-ключом

```bash
# Запустить скрипт
sudo ./server-deploy-v5-enhanced.sh

# В меню:
1. Выбрать "Управление пользователями"
2. Выбрать "Add new user"
3. Ввести username: developer
4. Ввести пароль: ********
5. Выбрать роль: developer
6. Указать группы: docker,sudo

# Добавить SSH-ключ
echo "ssh-rsa AAAA..." | sudo tee -a /home/developer/.ssh/authorized_keys
```

### Пример 2: Установка веб-стека (Nginx + MySQL + PHP)

```bash
sudo ./server-deploy-v5-enhanced.sh

# В меню:
1. Выбрать "Развёртывание сервисов"
2. Отметить: [X] nginx
3. Отметить: [X] mysql
4. Отметить: [X] php
5. Нажать Enter
6. Подтвердить установку

# Наблюдаете за прогресс-баром
# После установки все сервисы автоматически настроены и запущены
```

### Пример 3: Мониторинг системы

```bash
sudo ./server-deploy-v5-enhanced.sh

# В меню:
1. Выбрать "Мониторинг системы"
2. Выбрать "htop - Мониторинг процессов"

# Горячие клавиши в htop:
# - F6: Сортировка
# - P: По CPU
# - M: По памяти
# - T: По времени
# - F9: Убить процесс
# - q: Выход
```

### Пример 4: MC-Style интерфейс

```bash
sudo ./server-deploy-v5-enhanced.sh

# В меню:
1. Выбрать "MC-Style интерфейс"
2. Запустится tmux с 3 панелями

# Навигация:
# Ctrl+B, затем стрелки - переключение между панелями
# В верхней панели: cd /etc/nginx && nano nginx.conf
# В левой нижней: видите логи в реальном времени
# В правой нижней: видите htop

# Отсоединиться: Ctrl+B, D
# Вернуться: tmux attach -t deploy-mc
```

---

## 🐛 Решение проблем

### Проблема: dialog не установлен

**Решение:**
```bash
sudo apt-get update
sudo apt-get install -y dialog
```

### Проблема: Потерян доступ после отключения root

**Решение:**
1. Подключитесь через консоль (VNC/IPMI)
2. Войдите под gate или другим sudo-пользователем
3. Восстановите настройки:
```bash
sudo passwd -u root
sudo nano /etc/ssh/sshd_config
# Измените: PermitRootLogin yes
sudo systemctl restart sshd
```

### Проблема: Не работает ProxyJump

**Проверьте:**
1. SSH-ключ добавлен в `/home/gate/.ssh/authorized_keys`
2. Права на .ssh директорию:
```bash
sudo chmod 700 /home/gate/.ssh
sudo chmod 600 /home/gate/.ssh/authorized_keys
```
3. SSH-агент запущен на клиенте:
```bash
eval $(ssh-agent)
ssh-add ~/.ssh/id_rsa
```

### Проблема: Прогресс-бары не отображаются

**Решение:**
```bash
# Проверьте наличие dialog
which dialog

# Переустановите
sudo apt-get install --reinstall dialog

# Проверьте размер терминала (должен быть минимум 80x25)
echo $COLUMNS $LINES
```

---

## 📝 Логи и отладка

### Просмотр логов

```bash
# Лог скрипта
sudo tail -f /var/log/server-deploy.log

# Системный лог
sudo tail -f /var/log/syslog

# Лог SSH
sudo tail -f /var/log/auth.log
```

### Включение debug-режима

```bash
# Добавьте в начало скрипта
set -x  # Выводить все выполняемые команды

# Или запустите с debug
bash -x ./server-deploy-v5-enhanced.sh
```

---

## 🎓 Обучающие материалы

### Изучение tmux

```bash
# Шпаргалка по tmux командам
tmux list-keys          # Все горячие клавиши
tmux list-commands      # Все команды

# Полезные команды
tmux ls                 # Список сессий
tmux attach -t <name>   # Подключиться к сессии
tmux kill-session -t <name>  # Убить сессию
```

### Изучение dialog

```bash
# Примеры dialog
dialog --msgbox "Hello" 10 30
dialog --yesno "Continue?" 10 30
dialog --inputbox "Enter name:" 10 30
dialog --passwordbox "Password:" 10 30
dialog --menu "Choose:" 15 40 5 1 "Option 1" 2 "Option 2"
dialog --checklist "Select:" 15 40 5 1 "Item 1" off 2 "Item 2" on
```

---

## 🔄 Обновление

### Обновление до последней версии

```bash
# Скачать новую версию
wget https://your-repo/server-deploy-v5-enhanced.sh

# Сделать исполняемой
chmod +x server-deploy-v5-enhanced.sh

# Проверить версию
./server-deploy-v5-enhanced.sh --version
```

---

## 📞 Поддержка

### Получение помощи

1. **Встроенная справка:** Нажмите `H` в любом меню
2. **Логи:** `/var/log/server-deploy.log`
3. **Документация:** Файлы справки в `/srv/sys/help/`

### Известные ограничения

- Скрипт тестировался на Ubuntu 20.04/22.04 и Debian 11
- Требуется systemd (не работает на OpenRC)
- Некоторые функции требуют интернет-соединения

---

## 📄 Лицензия

MIT License - свободное использование и модификация.

---

## 👨‍💻 Автор

**Sandrick Tech**
- Версия: 5.0 Enhanced Edition
- Дата: Декабрь 2025
- GitHub: [ваш-профиль]

---

## 🎉 Благодарности

Спасибо всем, кто использует и улучшает этот проект!

Если у вас есть предложения или вы нашли баг - создайте Issue или Pull Request.

**Happy Deploying! 🚀**
