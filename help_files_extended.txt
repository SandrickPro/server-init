# Этот файл содержит расширенное содержимое для всех справочных файлов
# Для интеграции в server-deploy-v5-enhanced.sh

################################################################################
# MAIN.TXT - ГЛАВНАЯ СПРАВКА (РАСШИРЕННАЯ)
################################################################################
╔══════════════════════════════════════════════════════════════╗
║              СИСТЕМА РАЗВЁРТЫВАНИЯ СЕРВЕРА v5.0              ║
║                    ГЛАВНАЯ СПРАВКА                           ║
╚══════════════════════════════════════════════════════════════╝

НАВИГАЦИЯ ПО МЕНЮ:
- ↑/↓: Перемещение по пунктам
- Enter: Выбор пункта
- H: Вызов контекстной справки
- ESC/0: Выход

ОСНОВНЫЕ РАЗДЕЛЫ:

1. ПОЛЬЗОВАТЕЛИ
   - Создание и управление пользователями
   - Настройка SSH-ключей
   - Управление правами доступа
   - Квоты и ограничения

2. РАЗВЁРТЫВАНИЕ СЕРВИСОВ
   - Веб-сервер (Nginx, Apache)
   - Почтовый сервер (Postfix, Dovecot)
   - Базы данных (MySQL, PostgreSQL, MongoDB)
   - VPN-сервер (OpenVPN, WireGuard)
   - FTP-сервер (vsftpd, ProFTPD)
   - DNS-сервер (BIND9, Unbound)
   - Мониторинг (Netdata, Prometheus)

3. MC-STYLE ИНТЕРФЕЙС
   - Многопанельный интерфейс
   - Одновременное редактирование, мониторинг и просмотр логов
   - Использование tmux для управления панелями

4. МОНИТОРИНГ
   - htop: Интерактивный просмотр процессов
   - Системная статистика
   - Сетевые соединения
   - Логи в реальном времени

5. УСТАНОВКА С ПРОГРЕСС-БАРАМИ
   - Визуальные индикаторы прогресса
   - Параллельная установка компонентов
   - Отслеживание состояния установки

═══════════════════════════════════════════════════════════════
ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:
═══════════════════════════════════════════════════════════════

ПРИМЕР 1: Быстрая настройка веб-сервера
  1. Выберите "Развёртывание сервисов" → "Веб-сервер" → "Nginx"
  2. Дождитесь завершения установки (прогресс-бар покажет статус)
  3. Настройте виртуальный хост:
     nano /etc/nginx/sites-available/mysite.conf
  4. Включите сайт:
     ln -s /etc/nginx/sites-available/mysite.conf /etc/nginx/sites-enabled/
  5. Проверьте конфигурацию: nginx -t
  6. Перезапустите: systemctl reload nginx
  7. Откройте порт в firewall: ufw allow 80/tcp

ПРИМЕР 2: Создание нового пользователя с SSH
  1. "Пользователи" → "Добавить пользователя"
  2. Введите имя: webmaster
  3. Выберите роль: Администратор (sudo)
  4. Установите SSH-ключ:
     mkdir -p /home/webmaster/.ssh
     echo "ssh-rsa AAAA..." > /home/webmaster/.ssh/authorized_keys
     chmod 600 /home/webmaster/.ssh/authorized_keys
     chown -R webmaster:webmaster /home/webmaster/.ssh
  5. Тестируйте подключение: ssh webmaster@server-ip

ПРИМЕР 3: Настройка мониторинга
  1. "Развёртывание сервисов" → "Мониторинг" → "Netdata"
  2. После установки откройте: http://your-ip:19999
  3. Настройте алерты в /etc/netdata/health.d/
  4. Для Telegram уведомлений:
     nano /etc/netdata/health_alarm_notify.conf

ПРИМЕР 4: Использование MC-Style для отладки
  1. "MC-Style интерфейс" → Запустится tmux с 3 панелями
  2. Верхняя панель: nano /etc/nginx/nginx.conf
  3. Нижняя левая: tail -f /var/log/nginx/error.log
  4. Нижняя правая: htop (следите за nginx процессом)
  5. Переключение: Ctrl+B → стрелки

═══════════════════════════════════════════════════════════════
TROUBLESHOOTING - РЕШЕНИЕ ПРОБЛЕМ:
═══════════════════════════════════════════════════════════════

❌ ПРОБЛЕМА: "Permission denied" при установке
✅ РЕШЕНИЕ:
   - Убедитесь, что запустили скрипт от root:
     sudo ./server-deploy-v5-enhanced.sh
   - Проверьте права: chmod +x server-deploy-v5-enhanced.sh

❌ ПРОБЛЕМА: Прогресс-бар зависает на 0%
✅ РЕШЕНИЕ:
   - Проверьте интернет-соединение: ping -c 3 8.8.8.8
   - Обновите список пакетов: apt-get update
   - Проверьте логи: tail -f /srv/sys/logs/server-deploy.log
   - Проверьте файлы прогресса: ls -la /tmp/server-deploy/progress/

❌ ПРОБЛЕМА: Dialog/tmux не найдены
✅ РЕШЕНИЕ:
   - Система автоматически установит зависимости при первом запуске
   - Если нет, установите вручную:
     apt-get install -y dialog tmux jq htop
   - Проверьте установку: which dialog tmux jq htop

❌ ПРОБЛЕМА: SSH-подключение отклонено после настройки
✅ РЕШЕНИЕ:
   - Проверьте статус SSH: systemctl status sshd
   - Проверьте конфигурацию: sshd -t
   - Просмотрите логи: tail -f /var/log/auth.log
   - Убедитесь, что порт открыт: netstat -tlnp | grep :22
   - Проверьте firewall: ufw status

❌ ПРОБЛЕМА: Не могу войти через gate-пользователя
✅ РЕШЕНИЕ:
   - Проверьте SSH-ключ: cat ~/.ssh/authorized_keys
   - Права должны быть: 700 для .ssh, 600 для authorized_keys
   - Проверьте владельца: ls -la ~/.ssh
   - Проверьте конфиг SSH: grep -i "AllowUsers" /etc/ssh/sshd_config
   - Тестируйте подключение с verbose: ssh -vvv gate@server-ip

❌ ПРОБЛЕМА: Сервис не запускается после установки
✅ РЕШЕНИЕ:
   - Проверьте статус: systemctl status <service>
   - Просмотрите логи: journalctl -u <service> -n 50 --no-pager
   - Проверьте конфигурацию: <service> -t (для nginx, apache)
   - Проверьте порты: netstat -tlnp | grep <port>
   - Проверьте SELinux: getenforce (должен быть Permissive или Disabled)

❌ ПРОБЛЕМА: MC-Style интерфейс не запускается
✅ РЕШЕНИЕ:
   - Установите tmux: apt-get install tmux
   - Проверьте наличие screen: apt-get install screen
   - Убедитесь, что у пользователя есть права на /dev/pts
   - Попробуйте запустить вручную: tmux new -s test

❌ ПРОБЛЕМА: htop не отображает данные
✅ РЕШЕНИЕ:
   - Обновите htop: apt-get install --reinstall htop
   - Проверьте /proc: ls -la /proc | head
   - Увеличьте refresh rate: htop -d 5 (каждые 0.5 секунд)

═══════════════════════════════════════════════════════════════
FAQ - ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ:
═══════════════════════════════════════════════════════════════

❓ Как ускорить установку компонентов?
💡 Система автоматически использует параллельную установку. Для дополнительного
   ускорения настройте локальное зеркало APT или используйте apt-cacher-ng.

❓ Можно ли установить несколько веб-серверов одновременно?
💡 Технически да, но они не могут одновременно слушать порт 80/443. Используйте
   разные порты или настройте один как reverse proxy для другого.

❓ Как откатить изменения после установки?
💡 Резервные копии конфигов находятся в /srv/sys/backups/. Восстановите их:
   cp /srv/sys/backups/config-name-DATE.backup /etc/...

❓ Где хранятся логи установки?
💡 /srv/sys/logs/server-deploy.log - основной лог
   /var/log/syslog - системный лог
   journalctl - для systemd сервисов

❓ Как проверить, что все компоненты установлены?
💡 Просмотрите состояние: cat /srv/sys/state.json | jq .
   Проверьте сервисы: systemctl list-units --type=service --state=running

❓ Можно ли использовать скрипт на CentOS/RHEL?
💡 Скрипт оптимизирован для Debian/Ubuntu (apt). Для CentOS/RHEL потребуются
   изменения (замена apt на yum/dnf, пути к конфигам).

❓ Как добавить свой сервис в меню?
💡 Отредактируйте функцию services_menu() в скрипте, добавьте пункт меню
   и создайте функцию install_your_service().

❓ Безопасно ли использовать root для установки?
💡 Установка требует root прав. После настройки отключите root-доступ через
   SSH и используйте gate-пользователя с sudo.

❓ Как проверить безопасность после установки?
💡 - Запустите: lynis audit system
   - Проверьте открытые порты: nmap localhost
   - Просмотрите активные соединения: netstat -tupln
   - Проверьте fail2ban: fail2ban-client status
   - Просмотрите логи аутентификации: last -20

❓ Что делать, если забыл пароль gate-пользователя?
💡 - Войдите через console (если локальный доступ)
   - Сбросьте пароль от root: passwd gate
   - Или используйте recovery mode при загрузке

РЕКОМЕНДАЦИИ:
1. Всегда делайте резервные копии перед изменениями
2. Проверяйте логи после установки сервисов
3. Используйте H для вызова контекстной справки
4. Создайте gate-пользователя для безопасного доступа
5. Отключите root-доступ после настройки
6. Регулярно обновляйте систему: apt-get update && apt-get upgrade
7. Мониторьте дисковое пространство: df -h
8. Настройте автоматические backup'ы
9. Документируйте все изменения
10. Используйте fail2ban для защиты от брутфорса

ГОРЯЧИЕ КЛАВИШИ:
- H: Справка по текущему контексту
- Tab: Переключение между элементами
- Space: Выбор/Снятие выбора (чекбоксы)
- Enter: Подтверждение
- ESC: Отмена/Возврат

ПОЛЕЗНЫЕ ССЫЛКИ:
- Документация Nginx: https://nginx.org/ru/docs/
- Документация SSH: https://man.openbsd.org/sshd_config
- Безопасность Linux: https://www.cisecurity.org/cis-benchmarks/
- tmux cheatsheet: https://tmuxcheatsheet.com/


################################################################################
# USERS.TXT - УПРАВЛЕНИЕ ПОЛЬЗОВАТЕЛЯМИ (РАСШИРЕННАЯ)
################################################################################
╔══════════════════════════════════════════════════════════════╗
║           УПРАВЛЕНИЕ ПОЛЬЗОВАТЕЛЯМИ - СПРАВКА                ║
╚══════════════════════════════════════════════════════════════╝

Модуль для создания и управления учётными записями пользователей.

ДОСТУПНЫЕ ФУНКЦИИ:

1. Создание пользователей:
   - Обычные пользователи (без sudo)
   - Администраторы (с sudo-правами)
   - Системные пользователи (для сервисов)

2. SSH-ключи:
   - Генерация SSH-ключей (RSA 4096, Ed25519)
   - Установка публичных ключей
   - Управление authorized_keys

3. Sudo-права:
   - Полный доступ (ALL=(ALL) ALL)
   - Ограниченные команды
   - Отзыв прав

4. Квоты:
   - Установка лимитов на дисковое пространство
   - Soft и hard limits

5. ACL-права:
   - Детальное управление правами доступа
   - Права на уровне файлов и директорий

═══════════════════════════════════════════════════════════════
ПРИМЕРЫ КОМАНД:
═══════════════════════════════════════════════════════════════

ПРИМЕР 1: Создание нового администратора
  # Создание пользователя
  adduser --gecos "" admin1
  
  # Добавление в sudo группу
  usermod -aG sudo admin1
  
  # Установка SSH-ключа
  mkdir -p /home/admin1/.ssh
  chmod 700 /home/admin1/.ssh
  nano /home/admin1/.ssh/authorized_keys  # Вставьте публичный ключ
  chmod 600 /home/admin1/.ssh/authorized_keys
  chown -R admin1:admin1 /home/admin1/.ssh
  
  # Проверка
  su - admin1
  sudo whoami  # Должно вывести: root

ПРИМЕР 2: Создание пользователя с ограниченными правами
  # Создание пользователя
  adduser --gecos "" developer
  
  # Создание sudoers файла с ограничениями
  cat > /etc/sudoers.d/developer << 'EOF'
developer ALL=(ALL) NOPASSWD: /usr/bin/systemctl restart nginx
developer ALL=(ALL) NOPASSWD: /usr/bin/systemctl reload nginx
developer ALL=(ALL) NOPASSWD: /bin/cat /var/log/nginx/*
EOF
  
  chmod 440 /etc/sudoers.d/developer
  
  # Теперь developer может только перезапускать nginx и читать его логи

ПРИМЕР 3: Генерация SSH-ключей на сервере
  # Для пользователя admin1
  su - admin1
  ssh-keygen -t ed25519 -C "admin1@server" -f ~/.ssh/id_ed25519
  
  # Копирование публичного ключа на другой сервер
  ssh-copy-id -i ~/.ssh/id_ed25519.pub user@remote-server

ПРИМЕР 4: Установка квот на диск
  # Установка пакета
  apt-get install quota
  
  # Включение квот в /etc/fstab (добавьте usrquota,grpquota)
  /dev/sda1 / ext4 defaults,usrquota,grpquota 0 1
  
  # Перемонтирование и инициализация
  mount -o remount /
  quotacheck -cug /
  quotaon /
  
  # Установка квоты для пользователя (10GB soft, 12GB hard)
  setquota -u developer 10G 12G 0 0 /
  
  # Проверка
  quota -vs developer

ПРИМЕР 5: Настройка ACL для общей папки
  # Создание общей директории
  mkdir /srv/shared
  chown root:developers /srv/shared
  chmod 2775 /srv/shared  # Sticky bit + rwxrwxr-x
  
  # Установка ACL (пользователь user1 имеет полный доступ)
  setfacl -m u:user1:rwx /srv/shared
  
  # Установка ACL по умолчанию для новых файлов
  setfacl -d -m u:user1:rwx /srv/shared
  
  # Проверка
  getfacl /srv/shared

ПРИМЕР 6: Блокировка пользователя
  # Временная блокировка
  passwd -l username  # Lock password
  
  # Или через usermod
  usermod -L username
  
  # Разблокировка
  passwd -u username
  
  # Полное удаление пользователя с домашней директорией
  deluser --remove-home username

═══════════════════════════════════════════════════════════════
TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════

❌ ПРОБЛЕМА: "Username is not in the sudoers file"
✅ РЕШЕНИЕ:
   usermod -aG sudo username
   # Или добавьте в /etc/sudoers:
   echo "username ALL=(ALL:ALL) ALL" >> /etc/sudoers

❌ ПРОБЛЕМА: SSH-ключ не работает
✅ РЕШЕНИЕ:
   - Проверьте права: 700 для .ssh, 600 для authorized_keys
   - Проверьте владельца: chown -R user:user ~/.ssh
   - Проверьте формат ключа: должен начинаться с ssh-rsa или ssh-ed25519
   - Проверьте SELinux: restorecon -R -v ~/.ssh

❌ ПРОБЛЕМА: "Permission denied (publickey)"
✅ РЕШЕНИЕ:
   - Убедитесь, что PubkeyAuthentication yes в /etc/ssh/sshd_config
   - Проверьте логи: tail -f /var/log/auth.log
   - Проверьте права: ls -la ~/.ssh
   - Тестируйте с verbose: ssh -vvv user@server

❌ ПРОБЛЕМА: Квоты не работают
✅ РЕШЕНИЕ:
   - Проверьте /etc/fstab: должны быть usrquota,grpquota
   - Перемонтируйте: mount -o remount /
   - Инициализируйте: quotacheck -cug /
   - Включите: quotaon -av
   - Проверьте: repquota -a

БЕЗОПАСНОСТЬ:
- Все действия логируются
- Пароли хранятся в хешированном виде (SHA-512)
- SSH-ключи предпочтительнее паролей
- Регулярно проверяйте список пользователей: cat /etc/passwd
- Проверяйте последние входы: last -20
- Мониторьте sudo использование: tail -f /var/log/auth.log | grep sudo

ПОЛЕЗНЫЕ КОМАНДЫ:
- id username: Информация о пользователе (UID, GID, группы)
- groups username: Список групп пользователя
- w: Кто сейчас в системе
- last -20: Последние 20 входов
- lastb: Неудачные попытки входа
- sudo -l: Список доступных sudo команд
- passwd -S username: Статус пароля
- chage -l username: Информация о сроке действия пароля


################################################################################
# SERVICES.TXT - РАЗВЁРТЫВАНИЕ СЕРВИСОВ (РАСШИРЕННАЯ)
################################################################################
╔══════════════════════════════════════════════════════════════╗
║            РАЗВЁРТЫВАНИЕ СЕРВИСОВ - СПРАВКА                  ║
╚══════════════════════════════════════════════════════════════╝

Модуль для автоматической установки и настройки серверных компонентов.

ДОСТУПНЫЕ СЕРВИСЫ:

1. ВЕБ-СЕРВЕР (Nginx):
   - Nginx с модулями (GeoIP, cache-purge, headers-more)
   - SSL/TLS настройка (Let's Encrypt)
   - Rate limiting (защита от DDoS)
   - Виртуальные хосты
   - HTTP/2, QUIC/HTTP3
   Порты: 80 (HTTP), 443 (HTTPS)

2. ПОЧТОВЫЙ СЕРВЕР:
   - Postfix / Exim4 (MTA)
   - Dovecot (IMAP/POP3)
   - DKIM/SPF/DMARC (защита от спама)
   - ClamAV + SpamAssassin
   - Roundcube (webmail)
   Порты: 25 (SMTP), 465 (SMTPS), 587 (Submission),
          143 (IMAP), 993 (IMAPS), 110 (POP3), 995 (POP3S)

3. БАЗЫ ДАННЫХ:
   - MySQL 8.0 / MariaDB 10.11
   - PostgreSQL 15
   - MongoDB 7.0
   - Redis (кеш, очереди)
   Порты: 3306 (MySQL), 5432 (PostgreSQL), 27017 (MongoDB), 6379 (Redis)

4. VPN:
   - OpenVPN (с Easy-RSA PKI)
   - WireGuard (с QR-кодами для мобильных)
   - IKEv2/IPsec (strongSwan)
   - L2TP/IPsec
   Порты: 1194 (OpenVPN), 51820 (WireGuard), 500/4500 (IPsec)

5. FTP:
   - vsftpd / ProFTPD / Pure-FTPd
   - SSL/TLS шифрование (FTPS)
   - Passive mode для NAT
   Порты: 21 (FTP), 990 (FTPS), 20000-20100 (passive)

6. DNS:
   - BIND9 (с DNSSEC подписью)
   - Unbound (рекурсивный с DoT)
   - dnsmasq (легковесный для локальных сетей)
   - PowerDNS (с MySQL backend и web-интерфейсом)
   Порты: 53 (DNS), 853 (DoT - DNS over TLS)

7. МОНИТОРИНГ:
   - Netdata (real-time, красивый dashboard)
   - Prometheus + Grafana (метрики + графики)
   - Zabbix (enterprise мониторинг)
   - Icinga2 (форк Nagios)
   Порты: 19999 (Netdata), 9090 (Prometheus), 3000 (Grafana)

═══════════════════════════════════════════════════════════════
ПРИМЕРЫ КОНФИГУРАЦИЙ:
═══════════════════════════════════════════════════════════════

ПРИМЕР 1: Nginx с SSL (Let's Encrypt)
  # /etc/nginx/sites-available/mysite.conf
  server {
      listen 80;
      server_name example.com www.example.com;
      return 301 https://$server_name$request_uri;
  }
  
  server {
      listen 443 ssl http2;
      server_name example.com www.example.com;
      
      ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
      ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
      ssl_protocols TLSv1.2 TLSv1.3;
      ssl_ciphers HIGH:!aNULL:!MD5;
      
      root /var/www/mysite;
      index index.html index.php;
      
      location / {
          try_files $uri $uri/ =404;
      }
      
      location ~ \.php$ {
          fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
          fastcgi_index index.php;
          include fastcgi_params;
      }
  }
  
  # Получение SSL сертификата
  certbot --nginx -d example.com -d www.example.com

ПРИМЕР 2: MySQL безопасная установка
  # После установки MySQL запустите:
  mysql_secure_installation
  
  # Создание базы данных и пользователя
  mysql -u root -p
  CREATE DATABASE myapp;
  CREATE USER 'myapp_user'@'localhost' IDENTIFIED BY 'secure_password';
  GRANT ALL PRIVILEGES ON myapp.* TO 'myapp_user'@'localhost';
  FLUSH PRIVILEGES;
  
  # Настройка удалённого доступа (только для доверенных IP)
  # /etc/mysql/mysql.conf.d/mysqld.cnf
  bind-address = 0.0.0.0
  
  # Создание пользователя для удалённого доступа
  CREATE USER 'myapp_user'@'192.168.1.%' IDENTIFIED BY 'secure_password';
  GRANT ALL PRIVILEGES ON myapp.* TO 'myapp_user'@'192.168.1.%';
  
  # Firewall
  ufw allow from 192.168.1.0/24 to any port 3306

ПРИМЕР 3: WireGuard VPN сервер
  # /etc/wireguard/wg0.conf
  [Interface]
  Address = 10.8.0.1/24
  ListenPort = 51820
  PrivateKey = <server_private_key>
  PostUp = iptables -A FORWARD -i wg0 -j ACCEPT; iptables -t nat -A POSTROUTING -o eth0 -j MASQUERADE
  PostDown = iptables -D FORWARD -i wg0 -j ACCEPT; iptables -t nat -D POSTROUTING -o eth0 -j MASQUERADE
  
  [Peer]
  PublicKey = <client_public_key>
  AllowedIPs = 10.8.0.2/32
  
  # Генерация ключей
  wg genkey | tee server_private.key | wg pubkey > server_public.key
  wg genkey | tee client_private.key | wg pubkey > client_public.key
  
  # Запуск
  systemctl enable wg-quick@wg0
  systemctl start wg-quick@wg0
  
  # QR-код для мобильного клиента
  qrencode -t ansiutf8 < client.conf

ПРИМЕР 4: Postfix + Dovecot почтовый сервер
  # /etc/postfix/main.cf (основные настройки)
  myhostname = mail.example.com
  mydomain = example.com
  myorigin = $mydomain
  inet_interfaces = all
  mydestination = $myhostname, localhost.$mydomain, localhost, $mydomain
  
  # TLS шифрование
  smtpd_tls_cert_file=/etc/letsencrypt/live/mail.example.com/fullchain.pem
  smtpd_tls_key_file=/etc/letsencrypt/live/mail.example.com/privkey.pem
  smtpd_tls_security_level=may
  
  # DKIM настройка
  apt-get install opendkim opendkim-tools
  opendkim-genkey -s mail -d example.com
  cat /etc/opendkim/keys/example.com/mail.txt  # Добавьте в DNS
  
  # Проверка
  echo "Test mail" | mail -s "Test" user@example.com
  tail -f /var/log/mail.log

═══════════════════════════════════════════════════════════════
TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════

❌ ПРОБЛЕМА: Nginx не запускается после изменения конфига
✅ РЕШЕНИЕ:
   nginx -t  # Проверка синтаксиса
   tail -f /var/log/nginx/error.log  # Просмотр ошибок
   # Типичные ошибки:
   # - Порт уже занят: netstat -tlnp | grep :80
   # - Нет SSL сертификата: проверьте пути к .pem файлам
   # - Синтаксическая ошибка: проверьте ; и { }

❌ ПРОБЛЕМА: MySQL: "Too many connections"
✅ РЕШЕНИЕ:
   # Увеличьте max_connections в /etc/mysql/mysql.conf.d/mysqld.cnf
   max_connections = 500
   # Перезапустите: systemctl restart mysql
   # Проверьте текущие подключения:
   mysql -e "SHOW PROCESSLIST;" | wc -l

❌ ПРОБЛЕМА: WireGuard клиент не может подключиться
✅ РЕШЕНИЕ:
   # На сервере проверьте:
   wg show  # Список пиров
   ufw allow 51820/udp  # Порт открыт?
   sysctl net.ipv4.ip_forward  # = 1?
   # На клиенте:
   ping 10.8.0.1  # Доступен ли VPN IP?
   wg show  # Есть ли handshake?

❌ ПРОБЛЕМА: Почта уходит в спам
✅ РЕШЕНИЕ:
   # 1. Настройте SPF запись в DNS:
   v=spf1 mx a ip4:YOUR_IP ~all
   
   # 2. Настройте DKIM (выше пример)
   
   # 3. Настройте DMARC:
   _dmarc.example.com TXT "v=DMARC1; p=quarantine; rua=mailto:dmarc@example.com"
   
   # 4. Настройте reverse DNS (PTR запись)
   
   # 5. Проверьте: https://mxtoolbox.com/

ПАРАЛЛЕЛЬНАЯ УСТАНОВКА:
Компоненты устанавливаются параллельно для ускорения процесса в 3-5 раз.
Прогресс отображается в реальном времени для каждого компонента.

ПОСЛЕ УСТАНОВКИ:
- Проверьте статус: systemctl status <service>
- Просмотрите логи: journalctl -u <service> -f
- Настройте firewall: ufw allow <port>
- Создайте резервную копию конфига: cp /etc/<service>/config /root/backups/

ПОЛЕЗНЫЕ КОМАНДЫ:
- systemctl list-units --type=service --state=running: Работающие сервисы
- netstat -tlnp: Открытые порты и процессы
- lsof -i :<port>: Какой процесс слушает порт
- ss -tulpn: Альтернатива netstat
- journalctl --since "10 min ago": Логи за последние 10 минут


################################################################################
# MONITORING.TXT - МОНИТОРИНГ (РАСШИРЕННАЯ)
################################################################################
╔══════════════════════════════════════════════════════════════╗
║               МОНИТОРИНГ СИСТЕМЫ - СПРАВКА                   ║
╚══════════════════════════════════════════════════════════════╝

Инструменты для наблюдения за состоянием сервера.

HTOP:
Интерактивный просмотрщик процессов в реальном времени.

ГОРЯЧИЕ КЛАВИШИ HTOP:
- F1/h: Справка
- F2/S: Настройки (столбцы, цвета, метры)
- F3//: Поиск процесса по имени
- F4/\\: Фильтр (показать только совпадения)
- F5/t: Древовидный режим (показать иерархию процессов)
- F6/<>: Сортировка по выбранному столбцу
- F7/]: Увеличить nice (понизить приоритет процесса)
- F8/[: Уменьшить nice (повысить приоритет)
- F9/k: Убить процесс (SIGTERM, SIGKILL)
- F10/q: Выход

- Space: Отметить процесс (для массовых операций)
- U: Показать только процессы выбранного пользователя
- K: Скрыть kernel threads (полезно для очистки списка)
- H: Показать/скрыть user threads
- P: Сортировка по CPU (по умолчанию)
- M: Сортировка по памяти
- T: Сортировка по времени выполнения
- I: Инвертировать порядок сортировки
- L: Показать открытые файлы процесса (lsof)
- S: Системные вызовы процесса (strace)

ИНФОРМАЦИЯ В HTOP:
ВЕРХНЯЯ ПАНЕЛЬ:
  CPU: Загрузка каждого ядра (0-100%)
    - Зелёный: user processes (обычные программы)
    - Красный: kernel processes (ядро системы)
    - Синий: low priority (nice > 0)
    - Голубой: virtual machines
    - Жёлтый: IRQ (аппаратные прерывания)
    - Оранжевый: soft IRQ
  
  Mem: Использование оперативной памяти
    - Зелёный: used (используется программами)
    - Синий: buffers (кеш для чтения/записи)
    - Жёлтый: cache (файловый кеш)
  
  Swp: Использование swap-файла подкачки
    - Если swap > 50% - мало памяти!
  
  Load average: Средняя загрузка за 1, 5, 15 минут
    - < количества ядер = нормально
    - > количества ядер = перегрузка
  
  Tasks: Количество процессов
    - running: сколько выполняется сейчас
    - sleeping: ожидают события
  
ОСНОВНАЯ ОБЛАСТЬ (СТОЛБЦЫ):
  PID: Process ID (уникальный идентификатор)
  USER: Владелец процесса
  PRI: Priority (приоритет, меньше = выше)
  NI: Nice value (-20 до 19, меньше = выше приоритет)
  VIRT: Виртуальная память (сколько запросил)
  RES: Resident memory (физическая память, реально используется)
  SHR: Shared memory (разделяемая с другими процессами)
  S: State (R=running, S=sleeping, Z=zombie, D=uninterruptible)
  %CPU: Процент использования CPU
  %MEM: Процент использования памяти
  TIME+: Суммарное время CPU (сколько процессор работал для процесса)
  COMMAND: Команда/процесс

═══════════════════════════════════════════════════════════════
ИНТЕРПРЕТАЦИЯ МЕТРИК:
═══════════════════════════════════════════════════════════════

🟢 НОРМАЛЬНЫЕ ЗНАЧЕНИЯ:
- CPU < 70% (постоянно)
- Load average < количества ядер
- Memory used < 80%
- Swap used < 10%
- Disk I/O wait < 10%

🟡 ПРЕДУПРЕЖДЕНИЕ:
- CPU 70-90% (длительно)
- Load average = количество ядер * 1.5
- Memory used 80-95%
- Swap used 10-50%
- Много процессов в состоянии D (uninterruptible sleep)

🔴 КРИТИЧЕСКАЯ НАГРУЗКА:
- CPU > 90% (постоянно)
- Load average > количества ядер * 2
- Memory used > 95%
- Swap used > 50%
- Out of memory (OOM killer активен)

АЛЕРТЫ:
Настройте уведомления в Netdata:
  /etc/netdata/health.d/cpu.conf:
    alarm: cpu_usage
    on: system.cpu
    lookup: average -1m
    warn: $this > 80
    crit: $this > 95

═══════════════════════════════════════════════════════════════
ДРУГИЕ ИНСТРУМЕНТЫ МОНИТОРИНГА:
═══════════════════════════════════════════════════════════════

NETDATA:
Real-time мониторинг через веб-интерфейс с красивыми графиками.
  URL: http://your-server:19999
  Фичи:
    - Обновление каждую секунду
    - 1000+ метрик из коробки
    - Алерты в Telegram, Email, Slack
    - Без баз данных (хранит в памяти)
    - Экспорт в Prometheus, Grafana

PROMETHEUS + GRAFANA:
Enterprise система сбора метрик и визуализации.
  Grafana: http://your-server:3000 (admin/admin)
  Prometheus: http://your-server:9090
  
  Настройка:
    1. Установите node_exporter на всех серверах
    2. Добавьте targets в /etc/prometheus/prometheus.yml:
       - targets: ['localhost:9100', '192.168.1.10:9100']
    3. Импортируйте dashboard в Grafana (ID: 1860 для node_exporter)

ZABBIX:
Enterprise мониторинг для больших инфраструктур.
  Фичи:
    - Агенты на всех серверах
    - Шаблоны для всех популярных сервисов
    - Триггеры и эскалации
    - Карты сети
    - Отчёты и SLA

═══════════════════════════════════════════════════════════════
ПОЛЕЗНЫЕ КОМАНДЫ:
═══════════════════════════════════════════════════════════════

CPU МОНИТОРИНГ:
  top -bn1 | head -20               # Снимок top
  mpstat 1 10                       # CPU статистика каждую секунду (10 раз)
  sar -u 1 10                       # CPU usage (требует sysstat)
  ps aux --sort=-%cpu | head -10    # Топ-10 по CPU

ПАМЯТЬ:
  free -h                           # Использование памяти (human-readable)
  vmstat 1 10                       # Виртуальная память (каждую секунду)
  smem -tk                          # Memory по процессам (требует smem)
  ps aux --sort=-%mem | head -10    # Топ-10 по памяти
  
  # Очистка кешей (только при необходимости!)
  sync; echo 3 > /proc/sys/vm/drop_caches

ДИСК I/O:
  iotop -o                          # Только активные процессы I/O
  iostat -x 1 10                    # Детальная I/O статистика
  df -h                             # Использование дисков
  du -sh /var/*                     # Размер директорий в /var
  lsof | wc -l                      # Количество открытых файлов
  
  # Поиск больших файлов (>1GB)
  find / -type f -size +1G -exec ls -lh {} \; 2>/dev/null

СЕТЬ:
  iftop -i eth0                     # Сетевой трафик в реальном времени
  nethogs eth0                      # Трафик по процессам
  ss -tupln                         # Открытые порты (современная альтернатива netstat)
  netstat -i                        # Статистика интерфейсов
  nload eth0                        # График трафика в терминале
  
  # Топ-10 соединений по IP
  netstat -ntu | awk '{print $5}' | cut -d: -f1 | sort | uniq -c | sort -rn | head -10

ЛОГИ:
  journalctl -f                     # Реал-тайм системные логи
  journalctl -u nginx -f            # Логи конкретного сервиса
  journalctl --since "1 hour ago"   # Логи за последний час
  tail -f /var/log/syslog           # Классический tail
  multitail /var/log/nginx/*.log    # Несколько логов одновременно (требует multitail)
  
  # Поиск ошибок в логах
  journalctl -p err -b              # Только ошибки с последней загрузки
  grep -i error /var/log/syslog     # Grep по keyword

ПРОЦЕССЫ:
  ps aux                            # Все процессы
  ps -ef --forest                   # Древовидный вид
  pgrep -l nginx                    # Найти PID по имени
  pidof nginx                       # PID процесса
  kill -15 <PID>                    # Graceful stop (SIGTERM)
  kill -9 <PID>                     # Force kill (SIGKILL)
  killall nginx                     # Убить все процессы nginx
  
  # Зомби процессы
  ps aux | awk '$8=="Z"'

═══════════════════════════════════════════════════════════════
TROUBLESHOOTING:
═══════════════════════════════════════════════════════════════

❌ ПРОБЛЕМА: Высокая CPU нагрузка
✅ РЕШЕНИЕ:
   1. Найдите процесс: htop (сортировка по CPU - клавиша P)
   2. Проверьте детали: ps aux | grep <PID>
   3. Если это нормальный процесс, понизьте приоритет: renice +10 <PID>
   4. Если подозрительный (майнер?), убейте: kill -9 <PID>
   5. Проверьте cron задачи: crontab -l
   6. Проверьте автозагрузку: systemctl list-units --type=service

❌ ПРОБЛЕМА: Мало памяти (OOM Killer активен)
✅ РЕШЕНИЕ:
   1. Найдите пожирателя памяти: ps aux --sort=-%mem | head -10
   2. Проверьте логи OOM: dmesg | grep -i "out of memory"
   3. Увеличьте swap: fallocate -l 4G /swapfile; mkswap /swapfile; swapon /swapfile
   4. Настройте swappiness: sysctl vm.swappiness=10
   5. Рассмотрите добавление физической RAM

❌ ПРОБЛЕМА: Диск заполнен (100%)
✅ РЕШЕНИЕ:
   1. Найдите большие файлы: du -h / | sort -rh | head -20
   2. Очистите логи: journalctl --vacuum-time=7d
   3. Удалите старые ядра: apt-get autoremove --purge
   4. Очистите APT кеш: apt-get clean
   5. Найдите удалённые но открытые файлы: lsof +L1

❌ ПРОБЛЕМА: Медленный диск (высокий I/O wait)
✅ РЕШЕНИЕ:
   1. Проверьте: iostat -x 1 10
   2. Найдите процессы: iotop -o
   3. Проверьте SMART статус: smartctl -a /dev/sda
   4. Рассмотрите использование SSD
   5. Оптимизируйте базы данных (индексы, запросы)

❌ ПРОБЛЕМА: Netdata не показывает данные
✅ РЕШЕНИЕ:
   systemctl status netdata
   tail -f /var/log/netdata/error.log
   # Проверьте порт: netstat -tlnp | grep 19999
   # Переустановите: bash <(curl -Ss https://my-netdata.io/kickstart.sh)

РЕКОМЕНДАЦИИ:
1. Настройте алерты для критических метрик
2. Мониторьте тренды (не только текущие значения)
3. Документируйте baseline (нормальные значения для вашего сервера)
4. Используйте графики для анализа проблем
5. Настройте логирование в централизованное хранилище (ELK, Graylog)
6. Регулярно проверяйте дисковое пространство
7. Мониторьте сертификаты SSL (срок действия)
8. Настройте мониторинг доступности (uptime)
9. Используйте correlation между метриками для диагностики
10. Храните исторические данные для анализа инцидентов
