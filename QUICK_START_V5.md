# 🚀 БЫСТРЫЙ СТАРТ - Server Deploy Master v5.0

## ⚡ За 5 минут до полностью настроенного сервера!

---

## 📋 Что нужно

- ✅ Ubuntu 20.04+ или Debian 11+
- ✅ Root-доступ
- ✅ Интернет-соединение
- ✅ SSH-ключ (для безопасного подключения)

---

## 🎯 Шаг 1: Настройка SSH-безопасности (2 минуты)

```bash
# Скачайте файлы (если ещё не скачали)
git clone https://your-repo/server-init.git
cd server-init

# Сделайте скрипты исполняемыми
chmod +x ssh-banner.sh setup-ssh-security.sh

# Запустите настройку безопасности
sudo ./setup-ssh-security.sh
```

**Что произойдёт:**
- ✅ Красивый градиентный баннер при входе
- ✅ MOTD очищен от рекламы Ubuntu
- ✅ Создан gate-пользователь для ProxyJump
- ✅ Root-доступ отключён
- ✅ Аутентификация только по SSH-ключам

**⚠️ ВАЖНО! Не закрывайте текущую SSH-сессию!**

---

## 🔑 Шаг 2: Добавьте свой SSH-ключ (1 минута)

### На вашем локальном компьютере:

```bash
# Если у вас нет SSH-ключа, создайте его:
ssh-keygen -t rsa -b 4096 -C "your-email@example.com"

# Скопируйте публичный ключ
cat ~/.ssh/id_rsa.pub
```

### На сервере:

```bash
# Добавьте ваш публичный ключ для gate-пользователя
sudo sh -c 'echo "ssh-rsa AAAA...ваш-ключ...== your-email@example.com" >> /home/gate/.ssh/authorized_keys'

# Проверьте права
sudo chmod 600 /home/gate/.ssh/authorized_keys
sudo chown gate:gate /home/gate/.ssh/authorized_keys
```

### Проверка подключения:

```bash
# В НОВОМ терминальном окне (не закрывайте старое!)
ssh gate@your-server-ip

# Должен показаться красивый баннер с градиентом
```

**✅ Если вход прошёл успешно - можно закрывать старую сессию**

---

## 🖥️ Шаг 3: Запуск Deploy Master (2 минуты)

```bash
# Сделайте скрипт исполняемым
chmod +x server-deploy-v5-enhanced.sh

# Запустите
sudo ./server-deploy-v5-enhanced.sh
```

**Увидите главное меню:**

```
╔═══════════════════════════════════════════════════════════════════╗
║   _____ ______ _____  _    _ ______ _____                         ║
║  / ____|  ____|  __ \| |  | |  ____|  __ \                        ║
║ | (___ | |__  | |__) | |  | | |__  | |__) |                       ║
║  \___ \|  __| |  _  /| |  | |  __| |  _  /                        ║
║  ____) | |____| | \ \| |__| | |____| | \ \                        ║
║ |_____/|______|_|  \_\\____/|______|_|  \_\                       ║
║                                                                    ║
║        DEPLOY MASTER - Enhanced Edition v5.0                      ║
║        MC-Style + Progress Bars + Parallel Install                ║
╚═══════════════════════════════════════════════════════════════════╝

╔══════════════════════════════════════════════════════════════╗
║                        ГЛАВНОЕ МЕНЮ                          ║
╠══════════════════════════════════════════════════════════════╣
║  1 👤 Управление пользователями                              ║
║  2 ⚙️  Развёртывание сервисов                               ║
║  3 🖥  MC-Style интерфейс                                    ║
║  4 📊 Мониторинг системы                                     ║
║  5 💾 Резервное копирование                                  ║
║  H 📖 Справка                                                ║
║  0 ❌ Выход                                                  ║
╚══════════════════════════════════════════════════════════════╝
```

---

## 🎬 Типичные сценарии использования

### Сценарий 1: Создание пользователя-разработчика

```
1. Запустить: sudo ./server-deploy-v5-enhanced.sh
2. Выбрать: "1. Управление пользователями"
3. Ввести username: developer
4. Ввести пароль: ********
5. Выбрать роль: developer
6. Группы: docker,sudo
7. Готово! ✅

Затем добавить SSH-ключ:
sudo sh -c 'echo "ssh-rsa AAAA..." >> /home/developer/.ssh/authorized_keys'

Подключение через gate:
ssh -J gate@server developer@localhost
```

### Сценарий 2: Установка веб-стека (Nginx + MySQL + PHP)

```
1. Запустить: sudo ./server-deploy-v5-enhanced.sh
2. Выбрать: "2. Развёртывание сервисов"
3. Отметить (Space):
   [X] nginx
   [X] mysql
   [X] php
4. Нажать Enter
5. Подтвердить: Yes
6. Наблюдать прогресс-бар:
   
   ╔═════════════════════════════════════════════╗
   ║  Установлено: 2 из 3                       ║
   ║  nginx: 100% ✅                             ║
   ║  mysql: 100% ✅                             ║
   ║  php: 85% ⏳                                ║
   ║  [████████████████░░░░░] 85%               ║
   ╚═════════════════════════════════════════════╝

7. Готово! Все сервисы настроены и запущены ✅
```

### Сценарий 3: MC-Style мониторинг

```
1. Запустить: sudo ./server-deploy-v5-enhanced.sh
2. Выбрать: "3. MC-Style интерфейс"
3. Увидите 3 панели:

   ┌──────────────────────────────────────────┐
   │   РЕДАКТОР (/etc) - nano/vim             │
   ├────────────────────┬─────────────────────┤
   │  ЛОГИ (tail -f)    │  HTOP (мониторинг)  │
   └────────────────────┴─────────────────────┘

Управление:
- Ctrl+B, стрелки: переключение панелей
- Ctrl+B, D: отсоединиться (сессия продолжит работу)
- Вернуться: tmux attach -t deploy-mc
```

### Сценарий 4: Просмотр статистики системы

```
1. Запустить: sudo ./server-deploy-v5-enhanced.sh
2. Выбрать: "4. Мониторинг системы"
3. Выбрать: "1. htop - Мониторинг процессов"
4. В htop:
   - P: сортировка по CPU
   - M: сортировка по памяти
   - F9: убить процесс
   - q: выход
5. Или выбрать:
   "2. Статистика системы" - uptime, память, диски
   "3. Сетевые подключения" - netstat
   "4. Просмотр логов" - syslog, auth.log и др.
```

---

## 🔧 ProxyJump для доступа к пользователям

### На клиентской машине создайте `~/.ssh/config`:

```ssh-config
# Конфигурация для подключения через gate

# Подключение к gate
Host myserver-gate
    HostName 192.168.1.100      # IP вашего сервера
    User gate
    Port 22
    IdentityFile ~/.ssh/id_rsa

# Подключение к developer через gate
Host myserver-developer
    HostName 127.0.0.1           # localhost на сервере
    User developer
    ProxyJump myserver-gate
    IdentityFile ~/.ssh/id_rsa

# Подключение к admin через gate
Host myserver-admin
    HostName 127.0.0.1
    User admin
    ProxyJump myserver-gate
    IdentityFile ~/.ssh/id_rsa
```

### Использование:

```bash
# Подключение к gate
ssh myserver-gate

# Подключение к developer через gate (автоматически)
ssh myserver-developer

# Подключение к admin через gate
ssh myserver-admin

# Или напрямую без config:
ssh -J gate@192.168.1.100 developer@localhost
```

---

## 📖 Горячие клавиши

### В главном меню:
- `Стрелки` - навигация
- `Enter` - выбор
- `ESC` - выход/назад
- `H` - справка по текущему разделу

### В htop:
- `F1` или `h` - справка
- `F6` или `<>` - сортировка
- `P` - по CPU
- `M` - по памяти
- `F9` или `k` - убить процесс
- `q` - выход

### В tmux (MC-Style):
- `Ctrl+B` затем `%` - разделить вертикально
- `Ctrl+B` затем `"` - разделить горизонтально
- `Ctrl+B` затем `стрелки` - переключение панелей
- `Ctrl+B` затем `D` - отсоединиться
- `Ctrl+B` затем `X` - закрыть панель

---

## 🐛 Что делать если что-то пошло не так

### Проблема 1: Потерян доступ после отключения root

**Решение:**
1. Подключитесь через VNC/консоль провайдера
2. Восстановите root-доступ:
```bash
sudo passwd -u root
sudo nano /etc/ssh/sshd_config
# Измените: PermitRootLogin yes
sudo systemctl restart sshd
```

### Проблема 2: dialog не запускается

**Решение:**
```bash
sudo apt-get update
sudo apt-get install -y dialog whiptail tmux screen jq htop
```

### Проблема 3: ProxyJump не работает

**Проверьте:**
```bash
# 1. Права на .ssh
ls -la /home/gate/.ssh/
# Должно быть: drwx------ (700) для .ssh
# Должно быть: -rw------- (600) для authorized_keys

# 2. Исправьте если нужно
sudo chmod 700 /home/gate/.ssh
sudo chmod 600 /home/gate/.ssh/authorized_keys
sudo chown gate:gate /home/gate/.ssh -R

# 3. Проверьте ключ
sudo cat /home/gate/.ssh/authorized_keys
```

### Проблема 4: Прогресс-бар не показывается

**Решение:**
```bash
# Проверьте размер терминала (должен быть минимум 80x25)
echo "Columns: $COLUMNS, Lines: $LINES"

# Если меньше - увеличьте окно терминала или:
export COLUMNS=80
export LINES=25
```

---

## 📊 Проверка установки

### После выполнения всех шагов проверьте:

```bash
# 1. SSH-баннер работает
ssh gate@your-server  # Должен показать градиентный баннер

# 2. Root отключён
ssh root@your-server  # Должен отказать в доступе

# 3. Gate пользователь создан
id gate  # Должен вывести информацию

# 4. Скрипты работают
sudo ./server-deploy-v5-enhanced.sh  # Должно открыться меню

# 5. tmux установлен
tmux -V  # Должно показать версию

# 6. htop установлен
htop --version  # Должно показать версию
```

---

## 📞 Получение помощи

### Встроенная справка
- Нажмите `H` в любом меню для справки по текущему разделу
- Справка автоматически меняется в зависимости от контекста

### Логи
```bash
# Лог скрипта
sudo tail -f /var/log/server-deploy.log

# Системный лог
sudo tail -f /var/log/syslog

# SSH лог
sudo tail -f /var/log/auth.log
```

### Debug режим
```bash
# Запуск с debug-выводом
bash -x ./server-deploy-v5-enhanced.sh
```

---

## ✅ Чек-лист завершения

- [ ] SSH-баннер установлен и работает
- [ ] MOTD очищен от рекламы
- [ ] Gate-пользователь создан
- [ ] SSH-ключ добавлен для gate
- [ ] Root-доступ отключён
- [ ] ProxyJump настроен в ~/.ssh/config
- [ ] Deploy Master v5.0 запускается
- [ ] htop работает в меню
- [ ] MC-Style интерфейс запускается
- [ ] Прогресс-бары показываются при установке

**Если все галочки стоят - поздравляем! 🎉**

Ваш сервер полностью настроен и защищён!

---

## 🚀 Что дальше?

1. **Установите нужные сервисы**
   - Nginx, MySQL, PostgreSQL, Docker, и др.
   - Используйте параллельную установку

2. **Создайте пользователей**
   - Для разработчиков, администраторов
   - Настройте sudo-доступ

3. **Настройте мониторинг**
   - Используйте htop для процессов
   - MC-Style для комплексного обзора

4. **Создайте резервные копии**
   - Конфигураций
   - Баз данных
   - Важных данных

5. **Изучите документацию**
   - README_V5.md - полная документация
   - CHANGELOG_V5.md - что нового в v5.0
   - Файлы справки в /srv/sys/help/

---

## 📚 Полезные ссылки

- 📖 Полная документация: `README_V5.md`
- 📝 Список изменений: `CHANGELOG_V5.md`
- 🔧 Решение проблем: раздел "Troubleshooting" в README
- 💡 Примеры: раздел "Examples" в README

---

**Happy Deploying! 🚀**

*Sandrick Tech - Server Deploy Master v5.0*  
*Время настройки: ~5 минут*  
*Результат: Полностью защищённый и настроенный сервер*

---

## 🎁 Бонус: Шпаргалка команд

```bash
# === SSH ===
ssh gate@server                              # Вход через gate
ssh -J gate@server user@localhost           # ProxyJump

# === Управление сессиями tmux ===
tmux ls                                      # Список сессий
tmux attach -t deploy-mc                    # Подключиться к сессии
tmux kill-session -t deploy-mc              # Убить сессию

# === Мониторинг ===
htop                                         # Интерактивный top
iotop                                        # Дисковая активность
iftop                                        # Сетевая активность
netstat -tunapl                             # Сетевые подключения

# === Логи ===
tail -f /var/log/syslog                     # Системный лог
tail -f /var/log/auth.log                   # SSH логи
journalctl -u nginx -f                       # Логи nginx
journalctl -u mysql -f                       # Логи MySQL

# === Статус сервисов ===
systemctl status nginx                       # Статус nginx
systemctl restart nginx                      # Перезапуск nginx
systemctl enable nginx                       # Автозапуск nginx

# === Добавление пользователя через gate ===
sudo add-proxyjump-user username            # Helper-скрипт

# === Запуск Deploy Master ===
sudo ./server-deploy-v5-enhanced.sh         # Главное меню
```

Сохраните эту шпаргалку для быстрого доступа! 📋
